# React Native Skill Bridge

This legacy `.ai/skills` file is kept for older scaffold references.

Use the real Cursor project skill at `.cursor/skills/react-native/SKILL.md`.

The project skill is local guidance. It remains subordinate to `AGENTS.md`, `.cursor/rules/`, and the active scaffold.
